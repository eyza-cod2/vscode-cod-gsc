main() {

    // Structure field access should be on single line.
    level.
    aaa = 1;

    // Function calls with object should be on single line.
    a = level
    aaa();

    // Function calls with object should be on single line.
    aaa()
    thread bbb();



    // This is ok
    game[
        self aaa()
    ] = 1;
}